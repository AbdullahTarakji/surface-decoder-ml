# Phase 2 (data generation + EDA)
stim>=1.15.0
pymatching>=2.3.0
sinter>=1.15.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
jupyter>=1.0.0
streamlit>=1.20.0
# Phase 3 (neural decoders) — required to run Phase3_Iter1_MLP.ipynb
torch>=2.2.0
pytorch-lightning>=2.2.0
# Verified stack (conda env "ml"): torch 2.10.0+cu128, pytorch-lightning 2.6.1,
# numpy 2.3.5, stim 1.15.0, pymatching 2.3.1, scikit-learn / scipy 1.17.1.
# An NVIDIA Blackwell GPU (e.g. RTX 5080, sm_120) needs a CUDA 12.8 torch build.
# For bit-exact reproduction of reported numbers, pin these with == to the versions above.
